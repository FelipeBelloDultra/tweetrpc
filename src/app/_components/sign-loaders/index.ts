import { SignSkeletonHeader } from "./sign-skeleton-header";
import { SignSkeletonMain } from "./sign-skeleton-main";
import { SignSkeletonRoot } from "./sign-skeleton-root";

export const SignLoaders = {
  Root: SignSkeletonRoot,
  Header: SignSkeletonHeader,
  Main: SignSkeletonMain,
};
